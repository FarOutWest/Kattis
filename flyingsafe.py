#! usr/bin/env Python3
for i in range(int(input())):
    case = [int(a) for a in input().split()]
    cities, pilots = case[0], case[1]
    for j in range(pilots):
        ab = [int(a) for a in input().split()]
        citya, cityb = ab[0], ab[1]
    print(cities-1)
